<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finderfood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdicionarReceitasActivity"></activity>
        <activity android:name=".ReceitasSalvasActivity" />
        <activity android:name=".LoginEfetuadoImageViewActivity" />
        <activity android:name=".LoginEfetuadoActivity" />
        <activity android:name=".BuscarReceitasActivity" />
        <activity android:name=".ChatActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.finderfood.MessagesActivity" />
        </activity>
        <activity
            android:name=".ContactsActivity"
            android:label="@string/contacts">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.finderfood.MessagesActivity" />
        </activity>
        <activity android:name=".MessagesActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>